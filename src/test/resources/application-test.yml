spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/votacao_test
    username: votacao_test_user
    password: votacao_test_pass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.SQL: off
